#!/bin/bash
#
# swgemu (wip)

cd bin
EXTRA=$1

case $EXTRA in
  devtools ) ./devtools ;;
  install ) ./install ;;
  setup ) ./setup ;;
  start ) ./start ;;
#  stop )
  latest ) ./latest ;;
  build ) ./build ;;
esac
